import Vapor

/// Controls basic CRUD operations on `Todo`s.
final class TodoController {
//    /// Returns a list of all `Todo`s.
//    func index(_ req: Request) throws -> Future<[Pet]> {
//        return Pet.query(on: req).all()
//    }
//
//    /// Saves a decoded `Todo` to the database.
//    func create(_ req: Request) throws -> Future<Pet> {
//        return try req.content.decode(Pet.self).flatMap { pet in
//            return pet.save(on: req)
//        }
//    }
//
//    /// Deletes a parameterized `Todo`.
//    func delete(_ req: Request) throws -> Future<HTTPStatus> {
//        return try req.parameters.next(Pet.self).flatMap { pet in
//            return pet.delete(on: req)
//        }.transform(to: .ok)
//    }
}
